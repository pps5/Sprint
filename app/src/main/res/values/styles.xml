<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="cardButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:padding">12dp</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <style name="cardEditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:lines">1</item>
        <item name="android:inputType">textNoSuggestions</item>
        <item name="android:layout_marginStart">16dp</item>
        <item name="android:layout_marginEnd">16dp</item>
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:includeFontPadding">true</item>
        <item name="android:hint">@string/tap_to_input_goal</item>
        <item name="android:focusedByDefault">false</item>
        <item name="colorControlNormal">@android:color/transparent</item>
    </style>

    <style name="cardsTabLayoutStyle" parent="Widget.MaterialComponents.TabLayout">
        <item name="android:background">?attr/colorPrimary</item>
    </style>

    <style name="cardsArrowStyle">
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:padding">4dp</item>
        <item name="android:tint">@color/white</item>
    </style>
</resources>